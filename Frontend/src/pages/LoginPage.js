import React from 'react';
import AuthForm from '../components/auth/AuthForm';

/**
 * LoginPage Component
 * Page for user authentication (login/register)
 */
const LoginPage = () => {
  return <AuthForm />;
};

export default LoginPage;
